# vim: ft=sh
SUBCOMMAND_DESC="Shortcuts to handle CI server"
SUBCOMMAND_HELP=$(cat <<EOH

status [NAME]                    Should list the current status for pipelines
                                 or jobs in our CI server that we are interested
                                 in follow up. If NAME is given we should filter
                                 our pipeline name list by NAME.

EOH
)

function ci_status() {
  error "not implemented."
}

handle_interface ${@}
