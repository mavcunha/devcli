# vim: ft=sh

function _ngrok_path_check() {
  type -a ngroka &> /dev/null
  [[ $? != 0 ]] && in_red "Can't find 'ngrok', please install from 'https://ngrok.com/download' and add it to your PATH" && exit 1
}

_ngrok_path_check
