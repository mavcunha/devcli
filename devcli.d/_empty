# vim: ft=sh
# This file is empty on purpose, it will
# be used as default loading for missing
# libraries.
SUBCOMMAND_DESC="Empty library file"
SUBCOMMAND_HELP=$(cat <<EOH

It seems 'cmdmap' does not have a mapping for '${SUBCOMMAND}'.
Check: $(in_yellow "${CONFIGURATION_DIR}/cmdmap")
Ensure there's a mpping from '${SUBCOMMAND}' to an implementation.
EOH
)
